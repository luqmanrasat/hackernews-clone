type Link {
  id: ID! @id
  createdAt: DateTime! @createdAt
  url: String!
  description: String!
  postedBy: User
}

type User {
  id: ID! @id
  name: String!
  email: String! @unique
  password: String!
  links: [Link!]!
}